// The dashboard, while the route streams in.
import { PageSkeleton } from "@/components/dashboard/shared/PageSkeleton";

export default function Loading() {
  return <PageSkeleton kind="dashboard" />;
}
