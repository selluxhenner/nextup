// The raise page, while the route streams in.
import { PageSkeleton } from "@/components/dashboard/shared/PageSkeleton";

export default function Loading() {
  return <PageSkeleton kind="raise" />;
}
